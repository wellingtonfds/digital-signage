import { EcommerceModelType.Model } from './ecommerce-model-type.model';

describe('EcommerceModelType.Model', () => {
  it('should create an instance', () => {
    expect(new EcommerceModelType.Model()).toBeTruthy();
  });
});
