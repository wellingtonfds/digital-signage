import { EcommerceModelType } from './ecommerce-model-type.model';

describe('EcommerceModelType.Model', () => {
  it('should create an instance', () => {
    expect(new EcommerceModelType()).toBeTruthy();
  });
});
