import { EcommerceModel.Model } from './ecommerce-model.model';

describe('EcommerceModel.Model', () => {
  it('should create an instance', () => {
    expect(new EcommerceModel.Model()).toBeTruthy();
  });
});
